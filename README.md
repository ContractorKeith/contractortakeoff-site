# contractortakeoff-site

Static Astro site for `contractortakeoff.ai`, the marketing site for `contractor-bid`.

## Development

```sh
npm install
npm run generate:assets
npm run dev
```

## Checks

```sh
npm run build
npm run format:check
```

The site is designed for static deployment on Cloudflare Pages.
